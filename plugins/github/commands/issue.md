Create a new GitHub issue in the current repository using the `gh issue create` command.

Ask the user for:
1. Issue title
2. Issue body/description

Then create the issue using `gh issue create --title "..." --body "..."` and show the user the created issue URL.
